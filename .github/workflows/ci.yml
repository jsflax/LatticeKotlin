name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      # The conformance corpus lives on the LatticeCore conformance-corpus
      # branch; the runner (ConformanceCorpusTest, part of the native test
      # suite that `./gradlew build` runs) reads it from the submodule's git
      # object database without moving the release-tag pin.
      - name: Fetch conformance corpus
        run: git -C LatticeCore fetch origin conformance-corpus:refs/remotes/origin/conformance-corpus

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: gradle/actions/setup-gradle@v4

      - name: Build LatticeCore native library
        # clang explicitly: gcc rejects the bridge's in-class explicit member
        # specializations (managed_object.hpp); same pin latticecore's capi.yml
        # and latticepython's ci.yml use.
        env:
          CC: clang
          CXX: clang++
        run: python3 build.py

      - name: Build and test
        run: ./gradlew build --stacktrace

      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-${{ matrix.os }}
          path: |
            lattice-runtime/build/reports/tests
            lattice-runtime/build/test-results
